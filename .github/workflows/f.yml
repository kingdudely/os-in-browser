name: Open Public UDP Port (STUN)

on:
  workflow_dispatch:
    inputs:
      os:
        description: "Runner OS"
        required: true
        type: choice
        options:
          - ubuntu-latest
          - windows-latest
          - macos-latest
        default: ubuntu-latest
      local_port:
        description: "Local UDP port to bind"
        required: false
        default: "45000"
      duration_seconds:
        description: "How long to keep the port open (seconds)"
        required: false
        default: "3300"

jobs:
  open-port:
    runs-on: ${{ inputs.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install deps
        run: pip install aioquic cryptography

      - name: Run HTTP/3 test server (STUN + plain HTTP/3 response)
        run: >
          python3 http3_test_server.py
          --port ${{ inputs.local_port }}
          --duration ${{ inputs.duration_seconds }}