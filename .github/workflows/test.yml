name: Random Number Stream

on:
  workflow_dispatch:

jobs:
  stream:
    runs-on: ubuntu-latest
    steps:
      - name: Print random numbers forever
        run: |
          while true; do
            echo "Random number: $RANDOM"
            sleep 1
          done
