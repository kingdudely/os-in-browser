name: Test wait PID

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: windows-latest
    defaults:
      run:
        shell: bash

    steps:
      - name: Start 
        run: |
          pacman --help