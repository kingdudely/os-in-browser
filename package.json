{
	"name": "os-in-browser.pages.dev-host",
	"main": "index.js",
	"type": "module",
	"devDependencies": {
		"electron": "^43.2.0",
		"node-addon-api": "^8.9.1",
		"prebuildify": "^6.0.1"
	},
	"dependencies": {
		"node-gyp-build": "^4.8.4"
	},
	"scripts": {
		"install": "node-gyp-build",
		"build": "prebuildify --napi --strip --name node.napi",
		"start": "electron ."
	}
}